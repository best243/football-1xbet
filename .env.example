# ── football-data.org (clé GRATUITE en 30 s sur football-data.org/client/register)
FOOTBALL_DATA_KEY=votre_cle_ici

# ── Port serveur
PORT=4001

# ── Mode mock : true = données simulées (si pas de clé API)
USE_MOCK=false
